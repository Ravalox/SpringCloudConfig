---
server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git: 
          uri: https://github.com/path/to/spring-cloudconfig-wa-tolls
          username: username
          password: password
#management.endpoints.web.exposure.include:*
management:
  endpoints:
    web:
      exposure:
        include: "*"